test:
  override:
    # maybe we will need tests in the future
    - echo 'ok!'

deployment:
  meteor-docs:
    branch: test-deploy
    owner: meteor
    commands:
      - ./deploy.sh github meteor docs master
